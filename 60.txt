(define a 100)
(define b 1)
	(let ((a 10) 
		  (b (+ a 1)) 
		  (c (- b 1)))
#ここではaが100,bが1の環境で評価され、aの値が10,bが101,cが0のフレームができる。
	 (let ((a c)
		   (b (+ a 1))
		   (c (let ((b 9))
			   (- b a))))
#aが10,bが101,cが0の環境で評価され、aが0,bが11,cが(bが9,aが10のフレームでb-a=-1
と評価された結果により)-1となったフレームができる。
	  (+ a b c)))
